/// Enum to define different preview types for windows in the window overview
enum WindowPreviewType {
  /// Card-style preview with a compact layout
  card,
  
  /// Table-style preview with rows and columns
  table,
}
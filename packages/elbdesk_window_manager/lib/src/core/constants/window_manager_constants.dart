/// Constants used throughout the window manager
class WindowManagerConstants {
  // Private constructor to prevent instantiation
  const WindowManagerConstants._();

  /// Mobile bottom bar height in pixels
  static const double mobileBottomBarHeight = 60.0;
  
  /// Desktop taskbar height in pixels
  static const double desktopTaskbarHeight = 48.0;
}